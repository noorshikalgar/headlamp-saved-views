# headlamp-saved-views

A [Headlamp](https://headlamp.dev) plugin that lets you save frequently used
cluster/resource/filter combinations and jump back to them with one click —
instead of repeating cluster → namespace → resource → filters every time.

A saved view is a reproducible Headlamp navigation state (cluster, resource
type, and filters), not just a bookmarked URL.

## 1. What this plugin does

Save something like "prod cluster → Pods → CrashLoopBackOff" once, then
reopen it from a **Saved Views** page in the sidebar instead of re-navigating
and re-filtering every time.

## 2. Screenshots

Not yet available. This is a new plugin; screenshots will be added once it
has had real-world use. See [Manual test procedure](#9-manual-test-procedure)
below for how to see it running yourself in the meantime.

## 3. Requirements

- Headlamp with plugin support (see [Supported Headlamp versions](#4-supported-headlamp-versions))
- For development: Node.js 22+ and npm 11+

## 4. Supported Headlamp versions

Developed and tested against the plugin API shipped in
`@kinvolk/headlamp-plugin@0.14.0`. It has not been tested against other
versions — please open an issue if you try it on a different version and hit
problems.

## 5. Installation

**From the Headlamp Desktop Plugin Catalog** (once published to Artifact
Hub): open the Plugin Catalog inside Headlamp Desktop and search for "Saved
Views".

**Manual installation** (from a built tarball, e.g. a GitHub release asset):

```bash
tar xvf headlamp-saved-views-<version>.tar.gz -C ~/.config/Headlamp/plugins/
```

On Windows, extract into `%APPDATA%/Headlamp/Config/plugins` instead.

**In-cluster / web deployment**: copy the extracted plugin folder (containing
`main.js` and `package.json`) into the directory Headlamp's `-plugins-dir`
flag points at (default `/headlamp/plugins`) — typically via an init
container that populates a shared volume, the same pattern used by
[headlamp-k8s/plugins](https://github.com/headlamp-k8s/plugins).

Restart/reload Headlamp after installing.

## 6. Development

```bash
npm install
npm run start   # watches src/ and rebuilds; open Headlamp with this plugin's dev output loaded
```

Useful commands (see `AGENTS.md` for the full list, generated by the
official scaffold):

```bash
npm run lint      # eslint
npm run tsc       # type check
npm run test      # vitest
npm run build     # production build
npm run package   # produce the installable tarball
```

## 7. Configuration

No global settings are exposed by this plugin — there was nothing that
genuinely needed to be a cross-view setting for v1 (see `DECISIONS.md`).
Each saved view's own fields (name, description, cluster, resource,
filters, favorite) are configured when you create or edit it.

## 8. Usage

- **Create Saved View** — opens a blank form: name, description, cluster,
  resource type, and optional filters (namespaces, search text, label
  selector).
- **Save View** — a button in Headlamp's app bar, shown only when you're on
  a recognized built-in resource list (Pods, Services, ...) — it doesn't
  appear on Settings, Home, or dashboard-style pages where there's nothing
  useful to capture. Click it to open the create form prefilled with the
  cluster and resource kind. Namespace filter and search text are **not**
  auto-filled — see [Limitations](#10-limitations) for why — so add them
  yourself if you want them saved. The Saved Views page also has its own
  "Save Current View" button, for when you're already there.
- **Save a view for this &lt;kind&gt;** — a button inside a resource's own
  details view (works whether that's opened as a full page or Headlamp's
  Activity popup). Since a saved view always targets a list, not one
  specific resource, this prefills the resource's exact name as a search
  filter instead of trying to save "just this pod" directly — opening the
  result later narrows the list down to that one resource once you
  type/apply the search.
- **"Save View" while viewing logs** — Headlamp's pod logs viewer opens as
  its own separate Activity, distinct from the pod's details view, so the
  button above doesn't appear inside it. The app-bar "Save View" button
  handles this instead: if a specific pod's logs are open when you click
  it, it prefills that pod's exact name as a search filter automatically —
  no separate button to find inside the logs panel.
- **Open** — navigates to the saved cluster + resource list. If the view has
  namespace/search/label-selector filters, a tooltip on the Open button
  tells you what to apply manually after the page opens.
- **Edit** — change any field. `createdAt` is preserved; `updatedAt` bumps.
- **Duplicate** — creates a copy (suffixed "(copy)") to use as a starting
  point for a variant.
- **Favorite** — pins a view to the top of the list (favorites sort first,
  then most-recently-updated); also a sortable table column. Up to 5
  favorites additionally appear as direct links nested under **Saved
  Views** in the sidebar — one click, no need to open the table at all.
- **Delete** — requires confirmation; cannot be undone.
- **Search** — the search box on the Saved Views page filters your saved
  views by name/description/cluster/resource/filters. It does not search
  Kubernetes resources.

The list itself is a sortable table (click any column header), matching the
layout of Headlamp's own resource lists rather than a custom card design.

## 9. Manual test procedure

Automated tests cover the data/logic layer (see [Tests](#11-tests)) but a
few things only make sense to verify against a running Headlamp:

1. Install the plugin (see [Development](#6-development)) against a
   Headlamp instance connected to any cluster.
2. Navigate to a built-in resource list (e.g. Pods), set a namespace filter,
   and click **Save View** in the app bar — confirm cluster and resource
   kind are prefilled and namespace/search are blank with an explanatory
   note.
3. Fill in a namespace and search string, save it.
4. Go to **Saved Views** in the sidebar, confirm the view appears in the
   table and **Open** navigates to the correct cluster + resource list,
   with a tooltip listing the namespace/search to apply manually.
5. Edit the view, change its name, confirm `updatedAt` changes and the row
   re-sorts if you toggled favorite.
6. Duplicate it, confirm a "(copy)" entry appears with a new id.
7. Delete the duplicate — confirm the confirmation dialog appears and
   cannot be bypassed accidentally.
8. Reload Headlamp entirely — confirm the remaining saved view is still
   there (ConfigStore persistence survives reload).
9. If you can, remove/rename the cluster the saved view points at, or
   disconnect it — confirm the row shows a "cluster not currently
   configured" warning and Open is disabled, rather than crashing or
   redirecting to a different cluster.
10. Toggle Headlamp's dark theme — confirm the page and dialogs remain
    legible.
11. Test on an account with restricted RBAC (can't list namespaces) —
    confirm the form still works (namespaces are typed, not listed) and no
    error is thrown.

Steps 1–8 have been run against a real Headlamp instance (both Desktop and
the official Docker image) connected to a local `kind` cluster as part of
developing this plugin, and pass.

## 10. Limitations

- **Cluster identity is a name, not a stable ID.** Headlamp's public API
  only exposes the cluster's configured display name. Two different
  clusters that happen to share a name are indistinguishable to this
  plugin. See `DECISIONS.md` Decision C.
- **"Save Current View" only auto-fills cluster and resource kind.**
  Namespace filter and search text live in Headlamp's internal Redux store
  with no public read API, so they can't be captured automatically without
  depending on private internals — which this plugin deliberately avoids
  doing, so it keeps working across Headlamp upgrades. See `DECISIONS.md`
  Decision B.
- **Opening a saved view doesn't auto-apply namespace/search filters.**
  Built-in resource list routes don't carry that state in the URL, so this
  plugin navigates to the correct cluster + resource and tells you what to
  apply manually, rather than silently dropping the information.
- **Saved views are local to this browser profile.** They are not
  synchronized across machines and are not shareable as-is (a view
  referencing a cluster name may not resolve the same way elsewhere). Import
  /export is intentionally out of scope for this release — see
  `DECISIONS.md` Decision F.
- **CRD/custom resources aren't in the resource picker.** The resource list
  is built from Headlamp's built-in `K8s.ResourceClasses`; a saved view
  pointing at a CRD-backed resource that later disappears is handled
  gracefully (a "not available" warning), but CRDs can't currently be
  selected when creating a view.
- Maximum 200 saved views, 80-character names, 280-character descriptions
  (see `src/lib/limits.ts`) — chosen for UI/storage sanity, not arbitrary.

## 11. Tests

Unit tests cover validation, schema migration, CRUD operations, search,
sort, cluster-identity resolution, resource-catalog derivation, current-view
capture matching, and saved-view URL construction — see `src/**/*.test.ts`.
Run with `npm run test`. See [Manual test procedure](#9-manual-test-procedure)
for what requires an actual Headlamp instance.

## 12. Security & privacy

- No external network calls. No telemetry. No analytics.
- Saved views are stored only in this browser's Headlamp plugin
  configuration (via `ConfigStore`) and are never sent anywhere.
- No Kubernetes credentials or authentication tokens are ever read,
  stored, or transmitted by this plugin.
- Opening a saved view only navigates the UI — it never creates, modifies,
  or deletes any Kubernetes resource.
- This plugin never reads Kubernetes Secrets.

## 13. Contributing

See [CONTRIBUTING.md](./CONTRIBUTING.md).

## 14. Release process

1. Update `CHANGELOG.md` and bump the version in `package.json` (semver).
2. `npm run lint && npm run tsc && npm run test && npm run build && npm run package`.
3. Tag the release and attach the generated `.tar.gz` (and its printed
   sha256 checksum) as a GitHub release asset.
4. For Artifact Hub, ensure `artifacthub-repo.yml` and `artifacthub-pkg.yml`
   are present/updated per [Headlamp's publishing guidance](https://headlamp.dev/docs/latest/development/plugins/publishing/).

## License

[Apache-2.0](./LICENSE).

This is an independent, community plugin — it is not an official Headlamp
project (see [headlamp-k8s/plugins](https://github.com/headlamp-k8s/plugins)
for those) and is not affiliated with or endorsed by CNCF.

## Architecture notes

See [`DECISIONS.md`](./DECISIONS.md) for the design decisions that required
inspecting Headlamp's current plugin API/source rather than assuming an
answer — cluster identity, current-view capture, routing, and persistence.
